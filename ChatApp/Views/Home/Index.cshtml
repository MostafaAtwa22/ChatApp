@model IEnumerable<Chat>

<div class="chat-body">
    @foreach (var chat in Model)
    {
        <a class="group-btn" asp-controller="Home" asp-action="JoinGroup" asp-route-id="@chat.Id">
            @chat.Name
        </a>
    }
</div>